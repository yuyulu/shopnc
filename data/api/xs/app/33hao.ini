project.name = 33hao
server.index = 192.168.1.201:8383
server.search = 192.168.1.201:8384

[pk]
type = id

[goods_id]
type = numeric

[goods_name]
type = title
index = mixed
weight = 5
tokenizer = split(-)

[gc_name]
index = mixed
weight = 3

[goods_jingle]
index = mixed

[brand_name]
index = mixed
weight = 4

[cate_1]
index = self
tokenizer = full

[cate_2]
index = self
tokenizer = full

[cate_3]
index = self
tokenizer = full

[brand_id]
index = self
tokenizer = full

[is_book]
index = self
tokenizer = full

[attr_id]
index = self
tokenizer = split(_)

[store_id]
index = self
tokenizer = full

[area_id]
index = self
tokenizer = full

[have_gift]
index = self
tokenizer = full

[goods_promotion_price]
type = numeric

[goods_click]
type = numeric

[goods_salenum]
type = numeric

[goods_barcode]
index = self
tokenizer = full

[contract_1]
index = self
tokenizer = full

[contract_2]
index = self
tokenizer = full

[contract_3]
index = self
tokenizer = full

[contract_4]
index = self
tokenizer = full

[contract_5]
index = self
tokenizer = full

[contract_6]
index = self
tokenizer = full

[contract_7]
index = self
tokenizer = full

[contract_8]
index = self
tokenizer = full

[contract_9]
index = self
tokenizer = full

[contract_10]
index = self
tokenizer = full

[gc_id]
index = none

[main_body]
index = none
